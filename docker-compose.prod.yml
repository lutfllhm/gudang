services:
  mysql:
    image: mysql:8.0
    ports: []
    logging:
      options:
        max-size: "20m"
        max-file: "5"

  redis:
    image: redis:7-alpine
    ports: []
    logging:
      options:
        max-size: "20m"
        max-file: "5"

  backend:
    environment:
      NODE_ENV: production
    logging:
      options:
        max-size: "20m"
        max-file: "5"

  frontend:
    logging:
      options:
        max-size: "20m"
        max-file: "5"
